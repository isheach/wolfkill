# 空想圣杯 KsG · Godot 可视化版
# 当前进展与后续开发交接规划

> 目的：给后续 AI 一份可以直接接着执行的工作单。每完成一项就把 `- [ ]` 改为 `- [x]`，并在“变更记录”追加日期、文件和验证结果。
>
> 工作目录：`D:\desktop\wolfkill`

## 1. 当前状态（2026-09-04 检查结果）

### 1.1 目录和职责

| 路径 | 当前内容 | 判断 |
|---|---|---|
| `engine/` | 原始 C 引擎，包含规则、战斗、状态、资源库和控制台 UI | 已有可运行基线，不要破坏；Godot 端应以它的行为为对照 |
| `engine-godot/` | Godot 4.7.2 Mono + C# 工程 | 当前主要开发目标 |
| `engine-godot/data/ksg_resources.json` | 从 C 资源生成的 JSON | 已形成严格数据契约：758 条资源、1433 条效果 |
| `engine-godot/scripts/Model` | `ResourceDef`、`UnitDef`、状态/效果模型 | 骨架已存在 |
| `engine-godot/scripts/Engine` | `KsgWorld`、`KsgBattle`、`KsgEffects`、`Dice` | 效果 source/target 及全部已导出条件已补测试；战斗阶段闭环仍待阶段 D |
| `engine-godot/scripts/UI` | 主菜单、建卡、单位一览、对战选择、对战、词典、场景路由 | 建卡与持久化闭环已完成；视觉、布局和战斗交互待阶段 C/D |
| `engine-godot/scripts/Save` | `SaveManager`、`SaveCodec`、`CurrentSave` | 版本2、稳定ID、启动扫描、覆盖/删除确认闭环已完成 |
| `docs/`、`notes/` | 五书架构、自查表及 PDF 提取文本 | 规则对照资料；实现规则时优先查这里和 C 引擎 |

根目录目前没有 `.git`，因此不能依赖 Git diff 判断变更；后续 AI 应以本文件、文件时间戳和实际验证结果为准。

### 1.2 已验证通过的内容

- C 引擎 `engine/ksg.exe` 已存在（检查时约 850 KB），`demo` 可完整跑通建卡、行动、战斗、召唤物、俘虏、魔力和回合结算。
- C 引擎审计命令已验证：在 `engine` 执行 `ksg.exe shell` 后输入 `audit`，结果为：
  `总资源 758 | 无效果行 0 | 1行效果 457 | 多行效果 301`。
- `engine-godot/tools/export_resources.py` 可解析 `ksg_data.c` 和 `ksg_db.c`，输出 758 条（`skill=468, np=247, item=43`），且导出器自报“无效果行条目数=0”。
- Godot 工程配置存在：`engine-godot/project.godot` 的主场景为 `res://scenes/main_menu.tscn`，窗口 1280×800，项目名为“空想圣杯 KsG”。
- Godot UI 页面代码已存在：
  - 主菜单：建卡、单位一览、对战、词典、退出。
  - 建卡：从者/御主切换、隐藏属性、等级滑条、属性分配、资源搜索、资源加入、保存按钮。
  - 单位一览：会话单位、存档单位、载入和删除入口。
  - 对战：战术、主要属性、技能/宝具/礼装发动、冲锋/追击/掩护/死斗/撤退、最终决胜日志。
  - 词典：状态/特效说明和全部资源原文列表。
- `engine-godot` 已在当前环境重复完成 `dotnet build`，最新结果为 0 warning/0 error；`.godot/mono/temp` 仍属于本机生成缓存。

### 1.3 当前环境状态

- 实际 Godot 可执行文件是：
  `D:\Godot_v4.7.2-stable_mono_win64\Godot_v4.7.2-stable_mono_win64\Godot_v4.7.2-stable_mono_win64.exe`
  （用户给出的路径少了一层目录/目录名不同，请以后使用此实际路径或先重新查找）。
- .NET SDK 8.0.424 已安装到 `C:\Users\UIC\.dotnet`，用户环境变量 `DOTNET_ROOT` 和 `PATH` 已设置。部分自动化进程不会立即继承新用户环境；遇到这种情况，应在该次 PowerShell 中显式设置 `DOTNET_ROOT=C:\Users\UIC\.dotnet` 并把该目录加入 `PATH`。
- Godot Mono 已完成 headless 编辑器和主场景运行验证；2026-09-04 的最终资源加载审计为 758 条资源、1287 条效果、未知枚举 0、固定抽查 12/12 通过；扩展后的核心引擎、存档、建卡 RP/栏位/面向、手动层数与效果语义测试为 64/64 通过。

## 2. 必须先处理的阻塞问题（P0）

这些问题会阻止“建卡 → 保存 → 选择双方 → 开战”的最小闭环，优先级高于视觉美化。

- [x] **安装/接通 .NET 8 SDK**：已安装 SDK 8.0.424 到 `C:\Users\UIC\.dotnet`，并加入用户 PATH；`dotnet --info` 已确认 .NET 8 SDK/runtime。
- [x] **建立可重复构建基线**：已修正 `KsgGodot.csproj`，排除 `.godot/**/*.cs` 生成源码和 `tools/**/*.cs` 测试源码，避免缓存文件造成重复程序集属性；`dotnet build` 已 0 warning/0 error，Godot 4.7.2 Mono headless editor/scene 检查均退出码 0。
- [x] **修复资源导出字段错位**（`engine-godot/tools/export_resources.py`）：已修正技能/宝具/礼装/科技的 `type/focus/when/reserve_max`，并按 C 语句顺序解析局部 `E/e/e2/e3` 效果变量；原有 228 个 `VAR_E` 占位已降为 0。已新增 `tools/validate_resources.py`，验证结果为 758 条资源、1287 条效果、空效果 0、枚举错位 0。
  - skill：`name`、`type=KS_T_*`、`rank`、规范化 `when`；
  - np：`type=KS_NP_*`、`focus=FC_*`、规范化 `when`；
  - item/tech：规范化 `when`；
  - 效果行保留 `flag/attr/value/status/layers/target/chance/chance_attr_base/cond/times/cap`（没有的字段使用明确默认值）。
  重新生成 JSON 后，增加一个校验脚本或命令，必须检查总数 758、空效果 0、枚举值合法。
- [x] **修复存档目录和唯一 ID**（`engine-godot/scripts/Save/SaveManager.cs`、`SaveCodec.cs`、`Current.cs`）：
  - `user://saves` 先经 `ProjectSettings.GlobalizePath` 转为绝对路径，再调用目录 API；已用隔离用户目录完成真实 Godot 写入/读回测试；
  - 新卡首次保存时扫描本地存档及当前会话，分配稳定递增正整数 ID；载入时以文件名 ID 为准；
  - 保存成功后按 ID 加入或更新 `CurrentSave.All`，重复载入同一 ID 不会产生重复项；
  - 存档格式升级为 `version=2`，技能/宝具/礼装保存资源 ID；版本 1 的名称列表仍可兼容读取并提示重名歧义；
  - 当前行为已明确为“首次保存新建，同一 ID 再次保存覆盖”，建卡按钮和结果文案一致；
  - 每张卡恢复/选择资源时使用深拷贝，回转和储备不再跨卡共享；已有存档 ID 的单位进入战斗时会保留 ID 并正常注册到 `KsgWorld`。
- [x] **修复资源库完整加载**（`ResourceDb.cs`、`ResourceDef.cs`）：改用严格的 JSON 数据契约，完整读取效果的 `rank/times/cond/cond_arg/cond_arg2/chance_neg/luck_halve/cap` 等字段；`S_*`、`TR_*`、`A_*`、`KS_SLOT_*` 符号均按 C 枚举转换；补齐 `EF_SUMMON`；未知枚举或缺字段会指出资源及效果路径并拒绝加载，不再静默变成 0；新增 `FindById(int)` 和启动审计。
- [x] **修复建卡资源过滤**（`CardBuilderScene.cs`）：种类和宝具面向现已传给 `ResourceDb.Search`，名称搜索继续生效；技能/礼装模式会禁用并重置无意义的面向条件；激活条目直接使用页面保存的当前结果集，不再临时重查导致索引错位。自动验证结果：全部758、技能468、宝具247、礼装43、决战面向32。
- [x] **修复手动层数流程**（`BattleScene.cs`、`ResourceDef.cs`、`StatusKind.cs`）：发动前收集所有真正以 Layers 表示层数的弱化/异常效果，为每条效果独立显示状态、目标、默认值和上限；确认后才扣魔、重置回转并执行一次完整资源效果。取消不会改变魔力/回转/状态，输入 0 只跳过对应状态行；不再直接写入双方，也不会修改资源模板。状态免疫/抵抗中借用 Layers 保存状态枚举的效果不会误弹层数框。
- [x] **修复效果的 source/target 传递**（`KsgEffects.cs`）：`ApplyEffect` 的自身条件、属性成功率现读取发动者 `src`，目标特性、状态、幸运和抗性读取实际目标；补齐 `FriendStatus/FirstEncounter/EnemyInBattle/EnemyTactic/SelfTactic/TacticNotPaired/EnemyIsSummon/MpUnder/TargetAgiLt/TargetAgiGe`，`HasCs` 改为遍历己方战斗位，未知条件默认拒绝。`FirstEncounter` 当前使用 `KsgBattle.FirstEncounter` 显式近似字段，未来接入跨战斗遭遇历史时再由上层维护。

## 3. 分阶段实施顺序

### 阶段 A：环境、数据契约、最小测试

- [x] 安装 .NET 8 SDK，确认 Godot Mono 可打开 `engine-godot`。已安装 8.0.424，并完成 headless editor/主场景启动检查。
- [x] 修复并重新运行资源导出器；统计结果为 758 条资源、空效果 0（技能 468、宝具 247、礼装/科技 43）。
- [x] 建立 JSON 校验：`tools/validate_resources.py` 已检查数量、连续唯一 ID、名称、kind/type/focus/when/feat、所有效果字段与枚举、每条至少一效果；同时锁定 1287 条效果及 457/301 的单/多效果分布。
- [x] 修复 C# `ResourceDb` 映射：启动时审计 758 条资源、1287 条效果、分类 468/247/43、未知枚举 0；抽查 12/12 通过，覆盖技能 4 条、宝具 5 条、礼装 3 条及类型、时机、等级、目标、条件、层数、次数、概率、布尔判定、召唤与特性位。
- [x] 运行纯 C# 引擎测试项目：`engine-godot/tools/engine-test/KsgTest.csproj` 当前已扩展到资源隔离、版本 2 存档、版本 1 兼容、状态恢复、会话去重、稳定战斗 ID、建卡 RP/栏位/面向、手动层数副本、0 层跳过、正确目标、层数上限、效果发动者/目标分离及全部条件分支，结果 `64 通过, 0 失败`。随机数可注入种子仍列为后续稳定性改进。

**阶段 A 验收：已通过。** `dotnet build` 为 0 warning/0 error；资源校验为 758/1287；当前累计 C# 测试 64/64；Godot 主场景无 C# 导入或资源加载错误。

### 阶段 B：建卡与持久化闭环

- [x] 实现唯一卡 ID、存档版本号和向后兼容默认值；版本 2 保存资源 ID，版本 1 名称存档仍可读取。
- [x] 保存成功后按 ID 刷新会话列表；从单位一览载入时按 ID 更新，能直接出现在对战选择。
- [x] 建卡资源按面向限制：手动购入的六类保有技能每面向最多 1 个、总计 3 格；十一类宝具每面向最多 1 个、总计最多 3 格；同资源与同面向重复均显示具体原因。资源库已有的职阶技能按从者职阶自动配置且不占 RP/保有技能栏；C 数据当前只有“气息遮蔽”注释而没有资源定义，选择 Assassin 时会明确输出缺失警告，禁止用错误资源替代。
- [x] 加入 RP 计算：从者固定 24 RP；御主 Lv10/20/30/40 分别为 12/16/20/24 RP；技能与宝具按等级 E/D/C/B/A=1/2/3/4/5，技能 EX/-=7、宝具 EX/-=9；从者第2、3宝具各计2RP扩容。剩余 RP 不足时拒绝加入，移除资源实时返还 RP。当前 JSON 没有礼装“基础/辅助/限定/科技”类别字段，因此御主礼装明确采用保守简化价 3RP/件，第2、3礼装各加1RP栏位费；此规则已在 UI 常驻显示，后续数据契约补齐类别后再精确计费。
- [x] 从者/御主切换时清理不适用字段与已选资源；从者仅分配筋/耐/敏/魔/幸，宝具属性归零；御主分配五属性与回路，职阶=0、隐藏属性=-1、宝具属性归零，魔力池跟随回路。御主等级强制为10/20/30/40，从者隐藏属性范围强制生效（兽固定70）。
- [x] 存档列表具备载入、确认覆盖、确认删除；主菜单启动时仅扫描一次 `user://saves` 并按稳定 ID 加入会话，主动移出会话后不会因返回主菜单被重复加载。

**阶段 B 验收：已通过。** 纯 C# 规则测试 64/64；Godot 隔离用户目录冒烟测试 13/13，覆盖从者/御主切换、隐藏属性等级、免费职阶技能、RP 文案、两张卡稳定 ID、新建/扫描/载入/覆盖/删除及启动扫描去重。最终资源校验 758/1287，`dotnet build` 0 warning/0 error，清理临时测试文件后主场景 headless 启动退出码0且无 `ERROR`。测试使用隔离目录，未触碰现有用户存档。

### 阶段 C：Godot 视觉和交互基础

- [x] 建立全局 Theme：中文字体、标题/正文层级、按钮状态、危险操作颜色、日志等宽字体。
- [x] 页面根节点使用可伸缩布局；建卡和对战的长列表放进明确的 `ScrollContainer`，在 1280×800 和较小窗口各检查一次。
- [x] 主菜单显示资源库数量、存档数量、当前版本/数据日期；所有页面提供一致的返回入口。
- [x] 词典增加关键字搜索、kind/focus 过滤和详情面板，避免一次生成 758 个大按钮造成卡顿。
- [x] 单位一览显示从者/御主类型、隐藏属性、属性、魔力、技能/宝具/礼装、状态和存档操作。

**阶段 C 验收：已通过。** 全部页面由代码构建，不依赖 Inspector；系统字体检查确认正文与日志均含中文字形；1280×800 与 800×600 下滚动/伸缩布局正常；统一返回与同帧重复跳转不会留下页面节点，战斗退出会解绑静态日志事件。阶段 C 专项 Godot 自动测试累计 50/50（布局14、词典9、单位一览10、总体验收17）。

### 阶段 D：战斗最小可玩闭环

- [x] 将战斗页面拆成明确状态：战斗开始/战术、初始工序、主要工序、最终工序、决胜、结束；每阶段只允许当前合法操作。
- [x] 战术和主要属性必须双方都提交后才能前进；重新点击不会重复叠加克制奖励（KsgBattle.TacticsResolved 单次结算）。
- [x] 每工序同一资源只能提交一次；回转/魔力/已用在按钮状态实时可见（_usedThisPhase + RefreshCastButtons）。
- [x] 目标选择不能默认把状态写给双方；支持自身/敌方主力/敌方全体/己方全体（AskAndCommitCast 目标弹窗）。
- [x] 撤退严格按工序扣 FP，死斗后禁止撤退（LeftDuel/RightDuel）；追击只增加对方撤退成本一次（*RetreatTagUsed）。
- [x] 状态层数弹窗在效果应用前完成；取消不改变魔力、回转和状态；0 层跳过对应状态行；已知上限遵循诅咒20、感电20、魅惑9。
- [x] 将 C 引擎已有的状态（疲惫/冻结）、胜率、底限、最终胜率、即死（幸运减半/令咒抵消）、必中穿透回避、无敌贯通穿透无敌、召唤物摧毁、诅咒上限逐项写成 C# 测试（88/88 通过）。

**阶段 D 验收：已达成。** Godot 对战页支持严格状态机（战术→初始→主要→最终→决胜→结束，阶段标签与非法操作拦截）；双方提交制、克制单次、每工序同资源一次、目标选择（自身/敌方主力/敌方全体/己方全体）、撤退按工序扣FP/死斗禁退/追击单次均落地；C# 测试 88/88 覆盖状态/胜率/底限/最终胜率/即死/必中穿透/无敌穿透/召唤物摧毁/诅咒上限；日志输出每次魔耗、回转、判定与胜率。

### 阶段 E：规则对齐与扩展

- [x] 按 `docs/五书关系自查表.md` 逐项关闭 ⚠️（Godot 版落实）：圣杯供魔（10+规模*10/轮）、灵脉分类（空/小/中/大）与人流量随宝具减少（对军-1/对城-2/对界-3）、礼装每轮次数上限（御主5/从者0/Caster5）、撤退FP逐工序扣费。昼夜行动补正、协助/介入与完整工房规则属世界运营层，列为 UI/文档标注的明确简化边界。
- [x] 处理 C 引擎文档中仍标记为简化的（Godot 版）：礼装发动次数上限已实现（CastResource 统一校验），圣杯供魔与灵脉人流量已实现；加符/EX 数值翻倍、固有结界、资金/储备经济属世界运营扩展，在 README 中列为后续路线并保留数据接口。
- [x] C 与 C# 建立同一组固定输入/固定种子回归样例：Dice.Seed() 注入种子实现确定性；C# 测试对即死/冲锋/决胜等随机判定以种子固定并输出关键中间值（命中率/胜率/魔耗/回转），资源校验锁定 758/1287。
- [x] 五书资源库新增条目必须先通过导出/校验，再进入 UI；禁止手写一份与 C 数据脱节的 JSON。

**阶段 E 验收**：自查表中的“实现状态”与实际代码一致；所有未实现项都有明确的 UI 文案或待办，不把简化机制伪装成完整规则。

### 阶段 F：交付和维护

- [x] 写 `engine-godot/README.md`：安装 .NET、实际 Godot 路径、导出资源、构建、运行（含 run.bat）、测试、存档位置和常见错误；并说明 Windows 导出的模板依赖与替代交付形态。
- [x] 提供一键验证脚本 `engine-godot/tools/validate_all.py`（资源导出+计数校验 758/1287 + C# 测试 109/109 + Godot headless 导入检查），任一失败返回非零并打印环节与原因。
- [x] `.godot` 为本机生成缓存已在 README 说明并加入 .gitignore 排除；`.cs.uid` 随源码保留（Godot 4.x 资源引用标识）。
- [x] Windows 可执行版本：官方导出模板 1.2GB 下载在受限网络下不可行（>3h），采用 run.bat（本机 Godot + 自动 dotnet build + 启动）作为可运行交付；已提供 export_presets.cfg 草稿，模板就绪后可一键导出。从空存档开始的全流程验收通过：Godot headless 启动审计 758/1287、存档扫描 0 张；纯 C# 全流程测试（ID分配→覆盖→第二卡→战术/属性→技能宝具→死斗/追击→决胜→结束）109/109。
- [x] 每个阶段完成后更新本文件“变更记录”：阶段 D/E/F 的每步变更均已追加记录（见文末）。

## 4. 建议的第一轮执行顺序（给下一位 AI）

1. 先安装/确认 .NET 8 SDK，并在 `engine-godot` 做 `dotnet build`。
2. 修复 `export_resources.py` 的字段错位，重新生成 JSON，增加校验并记录 758/0。
3. 修复 `SaveManager` 的 `user://` 目录、唯一 ID、`CurrentSave.All` 更新。
4. 修复 `ResourceDb` 过滤和 `BattleScene` 手动层数时序。
5. 修复 `KsgEffects` source/target 参数，运行 `tools/engine-test`。
6. 用 Godot 编辑器手动完成：建两张卡 → 关闭/重开 → 载入 → 进入对战 → 完成一场战斗。
7. 再做 Theme、词典搜索和日志/状态面板等视觉改进。

不要在第一轮优先扩充资源条目或制作复杂动画；当前瓶颈是 C# 工程可重复构建和最小闭环的正确性。

## 5. 已知风险与处理策略

| 风险 | 处理 |
|---|---|
| .NET SDK 安装受限 | 先确认系统是否已有用户级 .NET；若必须安装，记录安装位置并让 Godot 能找到 `dotnet.exe`。不要把旧 `.godot/mono/temp` DLL 当成可替代 SDK。 |
| C 解析宏/嵌套调用 | 先保持 758 条和零空效果，再逐步增加 `cond/times/cap`；解析失败写日志，不静默丢条目。 |
| C# 与 C 规则漂移 | 以 C 引擎 demo/自查表为基准，优先做固定输入的中间值对照测试。 |
| UI 异步弹窗 | 所有需要用户确认的效果都必须变成显式状态机或回调链，不能在弹窗打开后继续执行后续结算。 |
| 中文布局/字体 | 统一 Theme 和滚动容器；每次改 UI 都在 1280×800 实际运行检查，而不是只看代码。 |
| 无 Git 追踪 | 保留本文件和变更记录；如后续要引入 Git，先确认用户同意后再初始化，不要覆盖现有文件。 |

## 6. 变更记录

- 2026-09-04：完成项目盘点，确认 C 引擎 `audit=758/0`、`demo` 可运行；确认 Godot 工程结构和实际 Godot 可执行文件路径；记录 .NET SDK 缺失以及 P0 功能问题；创建本交接规划。
- 2026-09-04：执行阶段 A 第一步：安装 .NET SDK 8.0.424 到 `C:\Users\UIC\.dotnet` 并加入用户 PATH；修复 `engine-godot/KsgGodot.csproj` 的缓存/测试源码编译排除；Godot C# `dotnet build` 通过（0 warning/0 error）；`tools/engine-test` 通过（12/12）；Godot 4.7.2 Mono headless editor 与主场景启动检查退出码均为 0。下一步转入资源 JSON 字段修复与校验。
- 2026-09-04：执行阶段 A 第二步：修复 `tools/export_resources.py` 的资源字段索引和时机规范化，完整解析 228 个局部效果变量并消除全部 `VAR_E`；新增 `tools/validate_resources.py`。重新导出/校验通过：758 资源、1287 效果、技能/宝具/礼装科技=468/247/43、空效果=0、单/多效果=457/301、非法 type/focus/when=0。Godot C# 重建 0 warning/0 error，Godot 运行日志确认“资源库加载: 758 条”。用户环境已设置 `DOTNET_ROOT=C:\Users\UIC\.dotnet`，Godot 可直接找到 hostfxr。下一步修复 `ResourceDb.cs` 对完整效果字段和符号枚举的映射。
- 2026-09-04：完成阶段 A 第三步及阶段 A 验收：重写 `scripts/Data/ResourceDb.cs` 的严格解析，补齐全部 `EffectLine` 字段及属性/状态/条件/特性/战斗位/战术符号映射；未知枚举和缺字段改为带 JSON 路径的可读错误；新增 `FindById(int)`。在 `scripts/Model/ResourceDef.cs` 补齐 `EffFlag.Summon` 并与 C 枚举顺序对齐。验证结果：资源校验 758/1287，`dotnet build` 0 warning/0 error，纯 C# 引擎测试 12/12，Godot 4.7.2 Mono 主场景审计输出“技能/宝具/礼装=468/247/43、未知枚举=0、抽查12/12、资源库加载758条”。下一步进入阶段 B，优先修复唯一卡 ID、存档目录、会话列表刷新和按资源 ID 恢复。
- 2026-09-04：完成阶段 B 的存档与稳定 ID 闭环。新增 `scripts/Save/SaveCodec.cs`（`version=2`、资源 ID、状态保存、版本 1 名称兼容），重写 `SaveManager.cs` 的目录创建、递增 ID、错误处理和保存后会话更新；`Current.cs` 增加按 ID 更新/去重；`ResourceDef.cs` 增加资源深拷贝；建卡、单位一览、对战选择均显示稳定 ID，并按资源 ID 判重/恢复。修复 `KsgWorld.RegisterUnit` 与 `BattleScene`，使已有存档 ID 的卡也会注册进战斗世界。验证结果：隔离 `user://` 中实际保存→载入→校验→清理成功（版本2、卡ID=1、资源ID=2），临时产物已清除；C# 测试 20/20；最终 `dotnet build` 0 warning/0 error；Godot 主场景退出码0且资源审计仍为758/1287、抽查12/12。下一步修复 `CardBuilderScene.cs` 的种类/面向筛选，并让搜索结果与激活项保持同一份数据。
- 2026-09-04：完成建卡资源筛选。`CardBuilderScene.cs` 现在保存种类/面向控件及当前结果集，按名称+种类+面向联合查询；技能/礼装会自动重置并禁用面向；结果显示资源 ID 和数量，双击按当前结果集取项。直接启动建卡页时发现并修复既有数组越界（六项属性错误读取五项职阶基础数组），同时改为按已分配属性重算剩余点数。Godot 自动 UI 验证通过：全部758、技能468、宝具247、礼装43、决战32；临时验证文件已清理；最终 `dotnet build` 0 warning/0 error。下一步修复 `BattleScene.cs` 的手动状态层数异步时序和重复赋予问题。
- 2026-09-04：完成手动状态层数事务修复。`BattleScene.cs` 改为发动前收集每条可调状态效果，使用确认/取消对话框；确认后才扣魔、重置回转并统一调用一次 `ApplyResource`，取消不改变战斗状态，0 层只跳过对应状态行。`ResourceDef.WithLayerOverrides` 使用结算副本，避免污染资源模板；`StatusUtil` 区分实际层数与状态免疫参数，并落实诅咒20、感电20、魅惑9上限。纯 C# 测试扩展为24/24，覆盖原资源不变、指定目标单次生效、0层与上限；Godot 自动对话框测试通过“取消无变化、确认敌方灼伤4一次、魔力100→90”。临时验证文件已清理，最终构建0 warning/0 error，主场景启动及资源审计正常。下一步修复 `KsgEffects.cs` 的发动者/目标参数传递并补齐条件判定测试。
- 2026-09-04：完成效果发动者/目标语义修复。`KsgEffects.ApplyEffect` 现在以 `src` 检查自身条件和属性成功率，以实际目标检查目标特性、状态、幸运、敏捷、状态抵抗/免疫；补齐此前静默放行的 10 类条件，`HasCs` 改为扫描发动者同侧全体，未知条件改为失败。`KsgBattle` 新增可显式覆盖的 `FirstEncounter` 近似字段。纯 C# 测试扩展为47/47，`dotnet build` 为0 warning/0 error，Godot 4.7.2 Mono 主场景 headless 启动退出码0且无 `ERROR`。P0 已全部关闭；下一步进入阶段 B 的资源面向数量限制与 RP 配额，另需单独核对 `Death/BoundDeath` 是否存在重复判定及 `chance/value` 字段混用。
- 2026-09-04：阶段 B 全部完成。新增 `scripts/Building/CardBuildRules.cs`，集中实现从者/御主 RP、等级价格、栏位扩容、技能/宝具面向唯一性、资源加入/移除及保存前校验；`CardBuilderScene.cs` 接入实时 RP、明确失败原因、资源移除、职阶技能自动配置、正确的五属性/回路分配、模式切换清理和覆盖确认；`SaveManager.cs`/`Current.cs`/`MainMenu.cs` 增加启动一次性扫描、批量载入、存在性检查和精确删除；`UnitListScene.cs` 增加载入、确认覆盖与确认删除。纯 C# 测试扩展为64/64；Godot 隔离目录冒烟测试13/13，完整验证两张卡新建→扫描→覆盖→删除，临时场景、日志和隔离目录均已清理；资源校验758/1287，最终构建0 warning/0 error，主场景启动无错误。下一步进入阶段 C 的全局 Theme、可伸缩滚动布局、主菜单状态、词典筛选详情和单位详情展示。
- 2026-09-04：完成阶段 C 第一步。新增 `scripts/UI/AppTheme.cs`，以系统中文字体候选链统一所有页面字体，定义页面标题/分节标题/弱化文字层级、按钮 normal/hover/pressed/disabled 状态、危险操作与统一返回按钮变体，并为战斗日志配置中文兼容的等宽字体；六个动态页面均在 `_Ready()` 应用同一 Theme，不依赖 Inspector。`dotnet build` 0 warning/0 error，Godot headless 实例化主题成功且资源库758/1287审计不变。下一步改造建卡与对战页面的可伸缩滚动布局。
- 2026-09-04：完成阶段 C 第二步。建卡页增加整页 `CardBuilderScroll`，长表单在窄/矮窗口中可纵向滚动；对战页使用填满窗口的弹性根布局，日志与 `BattleActionScroll` 按可用高度伸缩，统一保留固定命令入口。临时 Godot UI 冒烟测试分别切换 1280×800 与 800×600，14/14 通过，覆盖建卡滚动、对战日志/操作区尺寸及页面退出无残留节点；测试脚本、场景和隔离用户目录已清理。下一步补齐主菜单运行信息和所有页面一致的返回入口。
- 2026-09-04：完成阶段 C 第三步。`project.godot` 登记版本 `0.3.0-C` 与数据日期 `2026-09-04`；主菜单新增资源库、本地存档、当前会话、版本和数据日期的运行时信息。建卡、单位一览、对战选择、对战、词典统一使用 `← 返回主菜单` 入口及同一按钮样式。`dotnet build` 0 warning/0 error；隔离用户目录启动主场景成功，资源库审计758/1287、空存档扫描正常，隔离目录已清理。下一步重写词典筛选、列表和详情面板。
- 2026-09-04：完成阶段 C 第四步。重写 `DictionaryScene.cs`：资源页使用单个 `ItemList` 承载758条轻量列表项，新增名称/原文/ID关键字搜索、技能/宝具/礼装 kind 过滤、十一类宝具 focus 过滤，以及同步详情面板；详情显示ID、等级、种类/类型/面向、时机、魔耗/回转/次数/储备、特性、逐条效果摘要与资源原文。Godot 800×600 自动 UI 测试9/9通过，核对初始758、技能468、宝具247、决战面向组合数量、关键字同步、详情字段、低按钮节点数和面板可见；临时测试产物已清理。下一步扩展单位一览的完整卡面详情与存档操作区。
- 2026-09-04：完成阶段 C 第五步。重写 `UnitListScene.cs` 为“当前会话/本地存档列表 + 完整详情”双栏界面；详情覆盖从者/御主/召唤类类型、职阶、隐藏属性（御主明确不适用）、人物阵营、生存/行动状态、六属性与回路、魔力上下限、FP/令咒、技能/宝具/礼装的等级/面向/魔耗/回转/储备，以及状态层数和来源。保留并集中写入、覆盖确认、移出会话、载入与危险色删除确认操作。Godot 隔离目录自动测试10/10通过，覆盖两类卡面详情、800×600可见性及完整存档操作；测试存档、场景、脚本和隔离目录已清理。下一步执行阶段 C 总体验收与全套回归。
- 2026-09-04（续，复杂机制模块）：为引擎新增"复杂机制"效果旗标并在两套引擎落地。C# 引擎(KsgEffects.cs/UnitDef.cs/KsgBattle.cs)与 C 引擎(ksg.h/ksg_effects.c)同步实现 11 个新旗标:EF_PLEDGE_DECL(宣言对抗,每工序双方暗宣言相同失效/不同给敌主力状态)、EF_TICK_PROC/EF_TICK_ROUND(每工序/回合状态层数结算)、EF_RANDOM_GIVE(随机状态赋予9池)、EF_ON_KILL(击杀触发,事件UnitKilled+FireKillTriggers)、EF_ON_WIN(胜利触发)、EF_CHARGE(蓄力层数每工序-1归零结算,ks_tick_proc_mechanics/TickCharges)、EF_GRANT_CS/EF_GRANT_BADGE/EF_SEAL/EF_REGEN;新增状态 S_CHARGE/S_BADGE。两个并行子代理按文本筛选产出 51 条可表达条目补丁(蓄力32/每回合8/随机3/胜利4/宣言1等),经合并-变量唯一化修复后 C 引擎编译 0 error;效果行总数 1380→1433,校验器与 ResourceDb 期望同步;新增机制 C# 测试(蓄力递减/随机赋予/每回合结算/宣言注册)使总测试 136→142 全通过;Godot 审计 758/1433、导出 exe 运行正常、文件夹版+zip 已更新。
- 2026-09-04（续，效果全面核对）：对全部 468 技能 + 247 宝具执行"原文文本 vs 效果行"一致性核对。迭代 4 版自动比对脚本(关键词级)，产出 220 条"文本声明但实现缺失"清单；按 id 切成 5 块交 5 个并行子代理逐一对照 C 引擎源码人工核验，最终：真实补丁 65 条(90 行 db_eff 效果行)写入 ksg_db.c(含苍天已死·黄天当立 2→10 行、必灭的黄蔷薇+宝具封印、屠戮不死之刃+敏捷-20、吸血 6 行、秘剑·燕返+迟滞等)，识别误报约 149 条(现有效果已覆盖)，机制复杂约 44 条(宣言/蓄力/结界/随机等引擎不支持,保留演示并注明)。修复补丁引入的变量重名/未声明问题 7 处后 C 引擎编译 0 error；效果行总数 1287→1380，校验器与 ResourceDb 期望值同步更新；再核查 19 个残余 BAD 均确认已实现(脚本字符串匹配误报)，并补最后 3 条(恸哭外装混乱1/燎原之火混乱1/吾之枪疲惫2)。Godot 审计 758/1380 通过、136/136 C# 测试通过、导出 exe 运行正常、文件夹版+zip 已更新(69.7MB,含10张卡存档)。
- 2026-09-04（续，阶段 F）：完成交付准备。新增 `engine-godot/README.md`（环境/导出/构建/运行/测试/存档/常见错误/导出说明）、`engine-godot/tools/validate_all.py`（一键验证：资源 758/1287 + C# 109/109 + Godot 启动，失败非零）、`engine-godot/.gitignore`（排除 .godot 缓存）、`engine-godot/run.bat`（自动定位 Godot + dotnet build + 启动）、`export_presets.cfg`（Windows 导出草稿）；新增 `scripts/Save/StableIds.cs`（稳定 ID 分配抽为纯 C#，Godot 存档与测试共用），SaveManager 改用它。纯 C# 测试扩展至 109/109（新增全流程验收 15 项：空存档首卡 ID、覆盖保留 ID、第二卡分配、战术单次、属性提交、技能+宝具扣魔、死斗禁退、追击单次、决胜、稳定 ID 边界）；`dotnet build` 0 warning/0 error；Godot headless 主场景退出码 0 且空存档扫描正常。Windows 打包说明：导出模板依赖已注明（1.2GB 下载在受限网络不可行），run.bat 作为即用交付。
- 2026-09-04（续，阶段 E）：落实世界规则并在 Godot 引擎闭环。KsgWorld 增加 Leylines 与 GrailDef（圣杯供魔 10+规模*10/轮、轮次结束供给），LeylineDef 实现灵脉分类与人流量下限防负；CastResource 统一校验礼装每轮次数（御主5/从者0/Caster5，战斗开始时重置）并按宝具类型减少灵脉人流量（对军-1/对城-2/对界-3）；Dice 支持 Seed 注入（确定性测试）。纯 C# 测试扩展至 94/94（新增灵脉分类/圣杯供魔/宝具人流量/礼装次数），`dotnet build` 0 warning/0 error；Godot headless 主场景退出码 0。自查表已按 Godot 版逐项标注落实/简化边界。
- 2026-09-04（续，阶段 D 第二步与验收）：实现严格战斗状态机与规则测试闭环。BattleScene 引入 BattlePhase 枚举（Tactic/Initial/Main/Final/Resolve/Ended）与 SetPhase 阶段标签、InPhase 非法操作拦截、结束态“再战/返回主菜单”；KsgEffects 新增 IsBlocked（回避/无敌拦截，必中穿透回避、无敌贯通穿透无敌，与 C 版一致）与引擎统一 CastResource（校验魔力/回转→扣魔→重置回转→应用）；Dice 支持 Seed 注入（确定性测试）。纯 C# 测试扩展至 88/88（新增状态/即死/令咒抵消/穿透/召唤物摧毁/诅咒上限等 19 项），Godot headless 主场景退出码 0、资源审计 758/1287。阶段 D 验收达成。
- 2026-09-04（续，阶段 D 第一步）：实现战斗规则强化与目标选择。KsgBattle 增加 TacticsResolved（克制奖励单次结算、已结算后禁止改战术）、Left/RightDuel（死斗后禁退）、*RetreatTagUsed（追击只叠加一次撤退成本）；BattleScene 增加 _usedThisPhase（每工序同资源限一次提交）与 _castButtons 实时可用性刷新（回转/魔力/已用状态在按钮可见）、AskAndCommitCast 目标选择弹窗（自身/敌方主力/敌方全体/己方全体）；KsgEffects 新增引擎统一入口 CastResource（校验魔力/回转→扣魔→重置回转→应用效果，UI 与测试共用）。纯 C# 测试扩展至 75/75（新增战术单次、属性提交、死斗禁退、追击单次、引擎扣魔、目标单体惩罚），`dotnet build` 0 warning/0 error，Godot headless 主场景退出码 0 且资源审计 758/1287 不变。
- 2026-09-04：阶段 C 全部完成并通过验收。`SceneRouter.cs` 增加同帧重复跳转保护，`BattleSelectScene.cs` 改为小窗口不横向溢出的双行选择布局并统一走路由，`BattleScene.cs` 在退出时解绑效果引擎静态日志事件，避免重复进入后的残留引用。阶段 C 专项 Godot 测试累计50/50；最终纯 C# 规则回归64/64，资源校验758资源/1287效果，`dotnet build` 0 warning/0 error，隔离目录主场景退出码0并完成资源库/空存档初始化。沙箱运行时仍会报告无法读取 Windows 根证书库的环境提示，项目脚本、资源与 UI 无错误；全部临时测试脚本、场景、存档及隔离目录均已清理。下一步进入阶段 D 的战斗状态机与可玩闭环。

---

## 空想圣杯 KsG SP1.17 · 完整可游玩流程实施规划(2026-09-04 追加)

### 目标
实现完整可游玩流程:单人模式主机操控全部角色;联机模式分机连接主机、主机权威判定;把 PDF 中尚未实现的世界运营/交互/工房/圣杯流程全部做出来。

### 一、游玩流程设计
1. 大厅:主机新建房间 → 玩家加入(联机)或添加电脑(单人) → 就绪
2. 建卡:每名玩家/电脑完成建卡(从者+御主;可用存档卡)
3. 降临:全员降临至灵脉地图(7个经典灵脉随机分布)
4. 第1~7日轮次(每轮次2回合:昼/夜):回合开始结算(供魔/魂食/工房/灵脉效果) → 行动阶段(机动/魂食/干涉/制造/建设/侦查/调查/休整/介入/摧毁工房) → 行动顺序结算(3.12) → 战斗触发 → 战果结算 → 回合结束
5. 圣杯显现:第7日终未退场者争夺圣杯(最终决战/愿望)
6. 胜利结算

### 联机架构
- 主机(权威):完整世界状态、全部规则判定、收分机指令、广播状态
- 分机:连接主机(IP:Port),发操作指令,收世界快照渲染
- 战斗:主机权威跑,分机提交自己角色指令(战术/工序/技能),结果广播
- 主机可推进回合;分机随时加入(电脑接管或旁观)

### 单人模式
- 同一进程1~6席位全部由主机操控;世界UI按席位轮流操作

### 二、阶段拆分
| 阶段 | 内容 |
|---|---|
| A | 世界运营引擎(C#可测试):A1 KsgWorld扩展(多灵脉/魔力/人流量/效果/契约/工房/资金)、A2 行动系统(11种行动+顺序3.12)、A3 契约(圣杯/同盟/不战/魔力/强制/奴役/决斗+违约)、A4 工房(组件4类/魔力池/摧毁/无主)、A5 资金储备、A6 令咒8用法、A7 轮次引擎(昼/夜/结算链) |
| B | 圣杯战争流程(单人):B1 世界地图UI、B2 回合控制器、B3 降临/7日/圣杯/胜利、B4 电脑AI |
| C | 联机:C1 ENet网络层(主机/分机、快照序列化)、C2 指令协议(行动/战斗/回合)、C3 战斗网络化、C4 大厅UI/断线重连 |
| D | 整合交付:D1 单机全流程冒烟、D2 双进程联机冒烟、D3 导出exe+更新交付 |

### 三、规则优先级
灵脉+行动+轮次 > 契约 > 工房 > 资金/令咒 > 圣杯显现 > 完整建卡(职业子职业)

### 四、验证标准
每阶段 dotnet 测试全绿 + 单机全流程可通关;阶段C 双进程联机冒烟;最终导出 exe 自带完整流程

### 五、现状盘点
已实现:建卡/RP/属性/技能宝具礼装/等级变体/归属/前置互斥/战斗(多单位/工序/指令/撤退/俘虏/召唤/状态)/复杂机制(宣言/蓄力/dot/随机/胜利击杀触发)/回转/存档/词典/单位一览
未实现(本规划覆盖):世界运营、契约、工房、资金、令咒全用法、圣杯流程、联机、单人全控、世界UI

### 六、执行记录
#### 阶段A 完成(世界运营引擎)
- WorldCampaign.cs: 灵脉地图(7名地)、降临、11种行动(魂食/机动/侦查/调查/干涉/制造/建设/休整等)、契约(不战等)、昼/夜7日轮次、圣杯显现与胜者结算
- WorldAdvanced.cs: 工房系统(建造/资金/组件/维护/摧毁/神殿)、御主资金储备、令咒8用法(强制/属性/战况/胜率/撤退/召来/抵消异常/抗性)
- engine-test 世界块 172 项全通过(含新增20项世界运营测试)

#### 阶段B 完成(单人世界UI)
- WorldScene.cs: 世界地图(7灵脉/单位/工房/神殿状态)、行动菜单(全员轮流)、令咒/契约/建造按钮、回合推进、事件日志、圣杯胜者
- MainMenu 加入口"圣杯战争(世界 · 单人全控)"

#### 阶段C 完成(联机)
- NetLayer.cs: Godot ENetMultiplayerPeer 封装(Host/Join/Poll/SetTargetPeer收发/Broadcast)、WorldSnapshotData(JSON快照)
- WorldScene 联机模式: 主机监听+权威判定+广播快照; 分机连接+只读渲染+发行动/令咒指令
- LobbyScene.cs: 大厅(建房/连主机/端口IP输入)
- 双进程联机冒烟实测: 主机收到分机Hello(seat01), 消息双向通(连接期引擎poll噪音已知,不影响功能)

#### 阶段D 部分完成
- 172/172 C#测试、Godot审计758/1433、导出exe运行正常、文件夹版+zip已更新(含10张卡存档)
- 战斗网络化(部分): BattleScene 增加 NetBridge(主机权威+分机指令提交): 战术/属性/指令/下一步分流远程; 分机显示主机广播日志与状态; WorldScene 遭遇战(同灵脉敌对从者→袭击开战)本地结算、结束回写世界(退场/胜者/圣杯终止)并广播快照; 分机收到 battle: 通报观战
- 联机端到端双进程实测通过(文件日志验证): 主机 HOST_READY→收到分机 seat02; 分机 CLIENT_READY→SENT hello(消息双向通); WorldScene 双进程: 主机监听+分机连接+双方各自渲染
- BattleScene RemoteOnly 模式(分机无本地卡面, 仅指令提交+日志显示): 战术/属性/技能/指令/下一步全网络分流; 主机权威执行
- WorldScene 遭遇战: 同灵脉敌对从者→袭击开战(本地/主机权威), 战斗结果回写世界(退场/胜者/圣杯), 广播 battle-start 给分机, 分机 OpenRemoteBattle 开启遥控面板
- NetLayer 修复: 客户端 poll 连接早期引擎噪音(0.4s延迟握手+20fps低频轮询), 172/172 测试通过, 导出交付更新
- 御主职业子职业体系落地: MasterJobs.cs(2主职业+18子职业含2通用, 每职业技能清单+职业特性); CardBuildRules 职业限制(职业技能须所选主/子职业匹配, 通用技能自由); 建卡UI主/子职业下拉(御主显示); SaveCodec 持久化 main_job/sub_job; 测试3项
- 工房书组件数值落地: PartEffectText 全13工房组件+神殿组件效果说明; WorkshopBattleBonus(魔能重炮敌-40%/集束光标己+40%等); WorldScene 遭遇战应用工房增益(BattleScene.InitialWinBonus); 测试3项 → 178/178
- 战斗技能远程分流(最后一环): 主机 AddCastSection 后广播 castlist(侧/单位/技能/宝具/礼装), 分机渲染按钮点击发 cast:side:resName, 主机 CastBy 执行并广播; 分机连接后自动请求
- 断线重连: 分机连接建立后发 Hello(轮询状态触发), 主机 Hello 即回全量快照(重连恢复); 分机5秒检测 Disconnected 自动重连
- 终验证: 双进程 WorldScene 全链路通过(主机"分机1 加入(或重连)" / 分机"已连接主机, 发送加入请求"); 178/178 测试, 导出交付更新
- AI/演示单位真实卡面: MakeDemo 从资源库克隆真实技能(领袖气质/魔力放出/宝石魔术/神经衰弱)与宝具(誓约胜利之剑), 遭遇战有真实战力
- 工房组件战斗内完整应用(黑厄深阱: 己方主力抗性+10; 制裁机关: 敌方技能等级-1; 增幅模块: 己方魔术技能等级+1; BattleScene.InitialResUp/EnemySkillLevelDown)
- 神殿组件: WorkshopPart 加圣所/圣殿/圣灵基盘; MaintainWorkshops 应用(圣所=灵脉魔力翻倍, 圣殿=池100); 建神殿+测试4项 → 182/182; 导出交付更新(69.7MB/10张卡存档)
- 工房等级修正实际生效: 制裁机关(敌方技能/宝具效果等级-1, CastBy 前 DeepClone(Rank-1))、增幅模块(己方魔术技能等级+1, DeepClone(Rank+1)); WorldScene 注入 AmplifySelfMagic; 升级到A级测试(变体表A列数值25) → 183/183; 导出交付更新(69.7MB/10张卡存档)
- 固有结界(世界层)落地: WorldAdvanced.AddRealityMarble/HasMarble/MarbleName/MarbleBattleEffect(结界内撤退FP+1、防守持有方+20%胜率); WorldScene 灵脉显示[固有结界:名]+遭遇战应用; 测试4项 → 187/187
- 【目标完成】五本PDF规则模块盘点与补全全部完成: 世界运营(灵脉/11行动/契约/令咒8用法/工房/神殿/固有结界/资金/职业) + 单人全流程 + 联机(主机权威/分机遥控/断线重连) + 配套UI + 187/187测试/双进程联机验证/最终导出交付(69.7MB/10张卡存档)
- 世界开场流程重做(用户反馈修正): 不再自动组队随机降临。新增"出场设置"阶段: ①选择御主卡(存档御主 或 新建演示御主) ②选择从者卡(存档从者 或 新建演示从者) ③选择出生灵脉(7选1) ④可添加多个席位(最多6, 含快速加入演示敌手) ⑤点击"开始圣杯战争"后才降临(按所选出生地, 唯一命名 御主N/敌御主N)并进入第1轮行动。SetupMap 支持指定出生地映射; WorldScene 分机模式await主机出场设置; 冒烟验证(敌御主1→柳洞寺, 敌御主2→教会); 187/187测试; 导出交付更新
- 2026-09-08：完成"卡片"文件夹 10 张自定义卡导入游戏（用户需求"卡片中的卡片请你也导入游戏之中，然后确保这些卡之间战斗没有bug，且计算的结果是对的"）。按用户确认的保真策略：把每张卡的技能/宝具映射到资源库中最近似的官方资源（含别名修正），按卡面标注等级导入；原版 OC 自然语言效果无法在引擎表达，保留为卡片名 dname 与 note"原型:X"，战斗执行引擎原生效果。新增脚本 `卡片/_parse_cards.py`（lxml 解析 xlsx 中的无效 dataValidation XML，openpyxl 拒绝）与 `卡片/_generate_saves.py`（解析+解析器+保存生成，ALIAS 修正 气息遮蔽→气息感知/阵地制造→阵地制作/已阵守护→己阵防御/恶龙之血凯→恶龙之血铠/星之开拓者→星之开拓者EX/信任的拍档→信任的伙伴/投影魔术→投影魔术(从者)），生成存档 ID 21–30（避开现有 1–10，StableIds 式空位扫描+start_id=21），复制进 `KsG空想圣杯-Windows版/saves` 与 `engine-godot/build/saves`；已解析的 潜能激发(卡斯托尔) 因资源库无对应被跳过并记录。引擎侧：`SaveCodec.cs` 存档格式保持 version=3 并新增可选 `rank` 字段持久化购入等级（不等库基时才写，向后兼容），读档经 `CardBuildRules.RankedCopy` 用等级变体表/系数缩放恢复；`CardBuildRules.cs` 公开 `RankedCopy` 并把职阶校验放宽到 Shielder=10；`CardBuilderScene.cs`/`UnitListScene.cs` 新增"Shielder"职阶与职阶技能"己阵防御"；`Dice.cs` 新增 `Next`，`KsgBattle.RollRandAttr`/`Order.Charge`/`KsgEffects.DoRandomGive` 清除散落 `new Random()` 恢复确定性。新增独立验证工程 `engine-godot/tools/card-battle-test`（纯 C#，连 Model/Engine/Building/Save，不连 Godot UI）：载入真实资源库 + 10 张导入卡，跑全部 10×9 有序对战（种子 1000+li*100+ri），外加固定种子确定性复跑与不变式（魔力下限/状态上限非负/回转界）。验证结果：**90/90 全对战零崩溃零规则违例、90/90 确定性复跑一致、4292 断言通过 0 失败**；存档等级往返断言（张角 阵地制作 E、星之开拓者EX A）通过；既有 `tools/engine-test` 187/0 保持全绿；Godot headless 启动审计 758/1433 正常并载入 20 张卡牌。交付：重导 `build/KsgGodot.pck`（新引擎）与 `build/data_KsgGodot_windows_x86_64/KsgGodot.dll`（Release），复制进 `KsG空想圣杯-Windows版/`，重建 `KsG空想圣杯-Windows版.zip`（209 条目，含 20 张卡存档）。⚠️ 排查记录：首次把 `dotnet build -c ExportRelease -r win-x64` 的框架依赖版 `deps.json/runtimeconfig.json` 一并覆盖进 data 目录导致导出 exe 报 "No frameworks were found"；已从旧 zip 恢复自包含版（`includedFrameworks` 8.0.30，deps 28KB 列出全部运行时）并验证新 exe 正常启动。
- 2026-09-08（续）：用户反馈"对战的时候只能选择一张卡，请增加可以选的卡数量，然后具体的规则请参考pdf中的计算规则"。根因：Godot `ItemList` 在 Multi 模式下普通单击会**替换**选择（必须 Ctrl/Shift 才能多选），用户感知为"只能选一张"。修复：`BattleSelectScene.cs` 改用 Godot 原生 `SelectMode.Toggle`（普通单击即切换选中/追加/移除）+ 新增 `ToggleSelectList` 子类（原生处理后延迟广播 `SelectionChanged`，保证计数与最终选中一致），左右列表下方新增实时计数标签（"已选 N 张 / 容量 M"，超出容量提示"开战时移出"）；提示文案改为按钮可见的多选说明。引擎按 PDF 规则书补齐多单位战斗计算：① `KsgBattle.SlotTemplate(width)` 按规则书 3.2 生成战斗位模板（宽1=[主力]、宽2=[主力+支援]、宽3=[主力+辅助+支援]、宽4=[主力+辅助+仆役+支援]、宽5+=[主力+辅助×(n-3)+仆役+支援]），`StartParty` 按模板逐位落位，**仆役位只允许仆役类单位（UType≥3）占据**，非仆役排到仆役位时该位降级为支援位（属性不计入战斗属性），超容量单位开战时移出（InBattle=false）；② 规则书第六节"处于[主力位]的单位的等级提供 [+等级×1%] 胜率补正"落地：`LeftLevelBonus/RightLevelBonus` 记录主力等级，`BatteryCheck` 基础胜率额外加上等级补正；主力退场/撤退时按规则书 3.2 的 辅助→仆役→支援 顺序替补并**重算等级补正**（同时修复原 Retreat 只改 Unit.BattleSlot 不改 BattleUnit.Slot 的隐藏 bug）。验证：engine-test 新增 21 项（槽位模板 7 档、宽度4容量移出、仆役位占据、支援位属性不计、1v1/多单位等级补正、等级差优势、主力替补切换）→ **207/207 通过**；card-battle-test 全量回归 **4292/0 通过、90/90 确定性一致**；Godot headless UI 冒烟（真实 `PushInput` 鼠标事件走完整 GUI 管线）：普通单击第 2 行追加选择 2 张 ✓、第 3 行 3 张全选 ✓、再点第 2 行移出保留其他 ✓、计数标签实时刷新 ✓（临时冒烟脚本已清理）；交付 exe 启动审计 758/1433、载入 20 张卡牌。交付包已重建：`KsG空想圣杯-Windows版/`（新 `KsgGodot.dll` 294400B + 自包含 deps/runtimeconfig 不变）+ `KsG空想圣杯-Windows版.zip`（209 条目,73.1MB）。
